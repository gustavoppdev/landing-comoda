/**
 * 06 Depoimentos — spec.md §3.
 *
 * Sem `<h2>`: o COMODA.dc.html (fonte de verdade visual, spec.md §5) traz só o
 * rótulo "O QUE DIZEM" nesta seção. As falas entram na slice-8.
 */
export function Testimonials() {
  return <section id="testimonials" />;
}
