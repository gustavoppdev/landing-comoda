/**
 * Rodapé — spec.md §3.
 *
 * Sem heading. O wordmark, o aviso de projeto fictício e o link da política de
 * privacidade entram na slice-10.
 */
export function Footer() {
  return <footer />;
}
